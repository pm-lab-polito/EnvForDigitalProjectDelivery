complete_project_charter:
  properties:
    overview: { type: string }
    impact: { type: string }
    constraints:
      type: array
      items: { type: string }
    milestones:
      type: array
      items:
        type: object
        properties:
          date: { type: string }
          name: { type: string }
          description: { type: string }
        additionalProperties: false
  additionalProperties: false